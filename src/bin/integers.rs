
fn main(){
    // i8,i16,i32,i64,i128, and isize --> Signed Integer
    // u8,u16,u32,u64,u128, and usize --> Unsigned 

    // computer architecture에 따른 것

    // isize -> 32bit -> i32
    // usize -> 64bit -> i64

    let my_number:u8 =100;
    let my_other_number: i32 = 200;

    // let third_number = my_number + my_other_number;
    // 다른 타입끼리 더 할 수 없음

    // type inference


}